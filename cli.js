'use strict';

var should = require('should');

describe('cli', function() {
  it('should throw an error if binary does not exist', function(done) {
    //var cli = require('../lib/cli');
    should(true).be.exactly(true);
    done();
  });
});
